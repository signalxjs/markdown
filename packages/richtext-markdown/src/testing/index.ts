/**
 * `@sigx/richtext-markdown/testing` — the spec-conformance HTML renderer
 * (`toHtml`, byte-compatible with the CommonMark and GFM fixtures). The tree
 * helpers and the streaming harness are `@sigx/richtext/testing`.
 */

export { toHtml } from './html.js';
export type { ToHtmlOptions } from './html.js';
